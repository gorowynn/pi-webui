{
  "name": "pi-webui",
  "version": "0.1.0",
  "description": "Minimal, zero-dependency web UI for pi (--mode rpc).",
  "bin": { "pi-webui": "server.js" },
  "scripts": { "start": "node server.js", "test": "node test.js" }
}
